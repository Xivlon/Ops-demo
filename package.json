{
  "name": "luggster-ops",
  "version": "1.0.0",
  "description": "Operations console for LuggageLink with Neon DB integration",
  "main": "Luggster-Ops/Ops-Console.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
